services:
  user-service:
    build:
      context: ./user-service
      dockerfile: Dockerfile
    ports:
      - "8050:8050"
#    depends_on:
#      - postgres
      
#  postgres:
#    image: postgres
#    environment:
#      POSTGRES_DB: "user_info"
#      POSTGRES_USER: "city-discover-user"
#      POSTGRES_PASSWORD: "city-discover-2024"
#      PGDATA: "/var/lib/postgresql/data/pgdata"
#    volumes:
#      - pgdata:/var/lib/postgresql/data
#    ports:
#      - "5432:5432"

volumes:
  pgdata:
